void server_side::ServerSide(float side)
{
if ( fakelags::get().max_choke > 0 )
{
 side = get_globals()->m_curtime;
fakelags::get().max_choke < 16 && side; // max choke.
}
else
{
fakelags::get().max_choke = 0;
}
}
